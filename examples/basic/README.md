# Basic Example

This example demonstrates how to:
1. Connect to a CAN bus.
2. Add a remote node to the network.
3. Read the device name of the remote node.
4. Scan the network for other nodes.

## How to Run

Make sure you have a running CAN interface (e.g., `can0` or `vcan0`).

```bash
go run main.go
```
